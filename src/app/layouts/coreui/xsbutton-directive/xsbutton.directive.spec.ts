import { XsButtonDirective } from './xsbutton.directive';

describe('ButtonDirective', () => {
    it('should create an instance', () => {
        const directive = new XsButtonDirective();
        expect(directive).toBeTruthy();
    });
});
