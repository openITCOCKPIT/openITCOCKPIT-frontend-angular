import { ScoreSumPipe } from './score-sum.pipe';

describe('ScoreSumPipe', () => {
    it('create an instance', () => {
        const pipe = new ScoreSumPipe();
        expect(pipe).toBeTruthy();
    });
});
