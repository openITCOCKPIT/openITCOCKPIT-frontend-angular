import { TrustAsHtmlPipe } from './trust-as-html.pipe';

describe('TrustAsHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new TrustAsHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
