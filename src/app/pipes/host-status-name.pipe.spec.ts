import { HostStatusNamePipe } from './host-status-name.pipe';

describe('HostStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new HostStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
